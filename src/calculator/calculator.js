export class Calculator {
  add(a, b) {
    const result = a + b;
    return result;
  }

  subtract(a, b) {
    const result = a - b;
    return result;
  }

  multiple(a, b) {
    const result = a * b;
    return result;
  }

  divide(a, b) {
    const result = a / b;
    return result;
  }
}
